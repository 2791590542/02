#define _CRT_SECURE_NO_WARNINGS

#include<stdio.h>
int num1;
int num2;
Max(num1, num2)
{
	if (num1 > num2)
		return num1;
	else
		return num2;
}

int main()
{
	int a = 10;
	int b = 80;
	int c = 0;
	c=Max(a, b);

		printf("%d\n", c);
	
	return 0;
}
