#include<stdio.h>

void test()
{
	static int a = 1;//static：使局部变量的生命周期边长，不让变量a自动销毁
	a++;
	printf("a= %d\n", a);
}
int main()
{
	int i = 0;
	while (i < 5)
	{
		test();
	    i++;
	}
	system("pause");
	return 0;
